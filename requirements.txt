# Core phonetics
g2p_en           # grapheme-to-phoneme fallback for out-of-vocabulary words
syllabify        # CMUdict-backed syllable splitting
nltk             # corpora used by g2p_en (see `make install`)

# Data and analysis
pandas
numpy
matplotlib
seaborn
scikit-learn
scipy            # hierarchical clustering for the artist dendrogram

# Song ingestion
yt-dlp          # reads YouTube captions for lyrics + word timings
